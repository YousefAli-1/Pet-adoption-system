import { SettingsCustomValidators } from './settings-custom-validators';

describe('SettingsCustomValidators', () => {
  it('should create an instance', () => {
    expect(new SettingsCustomValidators()).toBeTruthy();
  });
});
